class AuthController {

    async Authentication() {
        let model = {
            usuario: ,
            senha:
        };

        if (model.senha == null || model.senha == undefined)
            console.log("Favor verificar sua senha");

        if (model.usuario == null || model.usuario == undefined)
            console.log("Favor verificar seu usuario");
    }

}